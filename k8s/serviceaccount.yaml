# Preserved from the OpenShift DeploymentConfig (serviceAccountName: task-manager).
# In OpenShift the SCC was bound to this ServiceAccount; under Kubernetes the
# security posture is enforced by namespace Pod Security Standards instead.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: task-manager
  labels:
    app: task-manager
