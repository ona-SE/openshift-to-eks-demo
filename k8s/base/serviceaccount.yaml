# Migrated from the ServiceAccount referenced in legacy-openshift/deploymentconfig.yaml
# (serviceAccountName: task-manager) and the SCC user binding in legacy-openshift/scc.yaml.
# TODO(human review): If running on EKS with IRSA, add the IAM role annotation:
#   eks.amazonaws.com/role-arn: arn:aws:iam::<account-id>:role/<role-name>
apiVersion: v1
kind: ServiceAccount
metadata:
  name: task-manager
  namespace: task-manager
  labels:
    app.kubernetes.io/name: task-manager
